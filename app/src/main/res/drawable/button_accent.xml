<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Pressed state -->
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <gradient
                android:startColor="@color/accent50"
                android:endColor="@color/accent70"
                android:angle="90"/>
            <corners android:radius="24dp"/>
        </shape>
    </item>

    <!-- Default state -->
    <item>
        <shape android:shape="rectangle">
            <gradient
                android:startColor="@color/accent60"
                android:endColor="@color/accent70"
                android:angle="90"/>
            <corners android:radius="24dp"/>
        </shape>
    </item>
</selector>
